name: 自動同步 byJoey/cfnew

on:
  push:
    branches:
      - main
  schedule:
    - cron: "0 1 * * *" # 每天凌晨1點運行
  workflow_dispatch:
    inputs:
      force_update:
        description: '是否強制更新（忽略版本檢查）'
        required: false
        default: 'false'

permissions:
  contents: write # 給予 workflow 寫倉庫的權限

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: 檢出您的倉庫
        uses: actions/checkout@v4

      - name: 檢查並同步最新 Worker
        env:
          REPO_API_URL: https://api.github.com/repos/byJoey/cfnew/releases
        run: |
          # 日誌函數
          log() { echo "[$(date +'%Y-%m-%d %H:%M:%S')] $1"; }

          log "開始檢查 byJoey/cfnew 更新..."

          # 獲取本地記錄的版本號
          LOCAL_VERSION=$(cat version.txt 2>/dev/null || echo "0.0.0")
          log "本地版本: $LOCAL_VERSION"

          # 獲取最新的 Release 完整資訊
          log "獲取最新 Release 資訊..."
          LATEST_RELEASE_INFO=$(curl -s "$REPO_API_URL" | jq '.[0]')
          LATEST_TAG=$(echo "$LATEST_RELEASE_INFO" | jq -r '.tag_name')

          if [ -z "$LATEST_TAG" ] || [ "$LATEST_TAG" == "null" ]; then
            log "ERROR: 無法從 byJoey/cfnew 獲取最新版本資訊"
            exit 1
          fi
          log "最新版本: $LATEST_TAG"

          # 判斷是否需要更新
          FORCE_UPDATE=${{ github.event.inputs.force_update || 'false' }}
          if [ "$LOCAL_VERSION" == "$LATEST_TAG" ] && [ "$FORCE_UPDATE" != "true" ]; then
            log "已是最新版本，無需同步"
            echo "updated=false" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          # 動態發現目標 ZIP 檔案
          log "動態發現目標 ZIP 檔案..."
          TARGET_ASSET_INFO=$(echo "$LATEST_RELEASE_INFO" | jq '.assets[] | select(.name | endswith(".zip") and (startswith("Source code") | not))')
          
          TARGET_FILE=$(echo "$TARGET_ASSET_INFO" | jq -r '.name')
          DOWNLOAD_URL=$(echo "$TARGET_ASSET_INFO" | jq -r '.browser_download_url')

          if [ -z "$TARGET_FILE" ] || [ "$TARGET_FILE" == "null" ]; then
            log "ERROR: 在版本 $LATEST_TAG 中未找到目標 ZIP 檔案！"
            exit 1
          fi
          log "成功發現目標檔案: $TARGET_FILE"
          log "下載連結: $DOWNLOAD_URL"

          log "發現新版本，開始下載..."
          wget -q -O "$TARGET_FILE" "$DOWNLOAD_URL"
          if [ $? -ne 0 ]; then
            log "ERROR: 下載 $TARGET_FILE 失敗"
            exit 1
          fi

          log "解壓縮 $TARGET_FILE..."
          unzip -o "$TARGET_FILE"

          log "清理已下載的 ZIP 檔案..."
          rm "$TARGET_FILE"

          log "更新本地 version.txt 檔案..."
          echo "$LATEST_TAG" > version.txt
          
          echo "updated=true" >> $GITHUB_OUTPUT
          echo "tag_name=$LATEST_TAG" >> $GITHUB_OUTPUT
          log "同步完成，新版本為: $LATEST_TAG"
        id: check_update

      - name: 提交更新到您的倉庫
        # 只有在 check_update 步驟的輸出 updated 為 true 時才執行
        if: steps.check_update.outputs.updated == 'true'
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "🔄 自動同步 Worker 版本: ${{ steps.check_update.outputs.tag_name }}"
          # --- 核心修改點: 指定提交者資訊，以觸發 Cloudflare Pages 部署 ---
          commit_user_name: GitHub Actions Bot # 您可以自訂這個名稱
          commit_user_email: ${{ github.actor_id }}+${{ github.actor }}@users.noreply.github.com
